#header
	%h5.home-link= link_to "No Bean Left Behind", "/"
	%ul.header-links
		- if current_user
			%li
				Signed in as 
				= link_to current_user.username, user_path(current_user.id)
			%li= link_to "Logout", logout_path
		- else
			%li= link_to "Login", login_path
